export enum SenderType {
  CUSTOMER = 'customer',
  STAFF = 'staff',
  ADMIN = 'admin',
  BOT = 'bot',
  SYSTEM = 'system',
}

export enum SupportStatus {
  OPEN = 'open',
  PENDING = 'pending',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
}

export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
}
